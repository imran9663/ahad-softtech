# Shared
NODE_ENV=development

# CMS / Database
DATABASE_CLIENT=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=ahad_softtech
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=change-me

# Strapi secrets - generate unique production values
APP_KEYS=replace-with-secure-random-values
API_TOKEN_SALT=replace-with-secure-random-value
ADMIN_JWT_SECRET=replace-with-secure-random-value
TRANSFER_TOKEN_SALT=replace-with-secure-random-value
JWT_SECRET=replace-with-secure-random-value

# Frontend
PUBLIC_CMS_URL=http://localhost:1337

# Analytics - configure later
PUBLIC_GA_ID=

# Optional Phase 1.3 custom role bootstrap
ENABLE_RBAC_BOOTSTRAP=false
