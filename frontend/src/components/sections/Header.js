import { esc } from '../../utils/content.js';
const fallback=[['About','/about'],['Services','/services'],['Case Studies','/case-studies'],['Careers','/careers'],['Insights','/blog']];
export function renderHeader(activePath='/',items=[]) { const nav=items?.length?items.map(n=>[n.label,n.url||n.slug||'#',n.openInNewTab]):fallback.map(n=>[n[0],n[1],false]); return `<header class="site-header" data-header><div class="container header-inner"><a class="brand" href="/" aria-label="AHAD Softtech home"><span class="brand-mark">A</span><span>AHAD<span class="brand-soft">Softtech</span></span></a><button class="menu-toggle" type="button" aria-expanded="false" aria-controls="site-nav" data-menu-toggle><span></span><span></span><span></span><span class="sr-only">Open navigation</span></button><nav id="site-nav" class="site-nav" aria-label="Primary navigation">${nav.map(([label,href,newTab])=>`<a class="nav-link ${activePath===href?'is-active':''}" href="${esc(href)}" ${newTab?'target="_blank" rel="noopener noreferrer"':''}>${esc(label)}</a>`).join('')}<a class="button button-small button-primary" href="/request-quote" data-track="request_quote">Request a Quote</a></nav></div></header>`; }
export function bindHeader(){const toggle=document.querySelector('[data-menu-toggle]');const nav=document.querySelector('#site-nav');if(!toggle||!nav)return;toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));nav.classList.toggle('is-open',!open);document.body.classList.toggle('nav-open',!open);});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{toggle.setAttribute('aria-expanded','false');nav.classList.remove('is-open');document.body.classList.remove('nav-open');}));}
